export function validateCreatePollPayload(req, res, next) {
    // Placeholder validator for upcoming poll APIs.
    // Add zod/joi validation here when implementing create poll endpoint.
    next();
}
